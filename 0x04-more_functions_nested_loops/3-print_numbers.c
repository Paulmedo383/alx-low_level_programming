#include "main.h"

/**
 * print_numbers -  prints the numbers
 * 0 to 9
 * Return: 0
 */

void print_numbers(void)
{
	char ch;
	int c;

	for (c >= 0; c <= 9; c++)
	{
		_putchar(ch);
	}
	_putchar('\n');
	return (0);
}
