#include "main.h"

/**
 * print_numbers -  prints the numbers
 * 0 to 9
 * Return: return nothing
 */

void print_numbers(void)
{
	if (c >= '0';)
	{
		_putchar('\n');
	}
	if (c <= '9';)
	{
		_putchar('\n');
	}
}
