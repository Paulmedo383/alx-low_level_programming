#include "main.h"

/**
 * print_numbers -  prints the numbers
 * 0 to 9
 * Return: return nothing
 */

void print_numbers(void)
{
	int c;

	for (c = '0'; c < '9' c++)
	{
		putchar(0);
	}
	putchar(9);
}
