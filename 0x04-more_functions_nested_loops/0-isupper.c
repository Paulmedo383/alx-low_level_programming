#includen "main.c"

/**
 * main - checks for uppercase character
 *
 * Return: 1 if c is uppercase, 0 otherwise
 */

int _isupper(int c);
{
	int c;

	if(c >= 'A';)
	{
		return(1);
	}
	else if(c <='Z';)
	{
		reuturn(1);
	}
	return(0)
}
